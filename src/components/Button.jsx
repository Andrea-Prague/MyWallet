import React from "react";

//here will be styles for round button and maybe props for color image instead of image in div?

const Button = props => <div color={props.color}>{props.image}</div>;

export default Button;
